flask
flask-cors
# Production WSGI server — used in containerized deploys (Fly.io etc.). The
# Flask dev server is not safe for production traffic.
gunicorn
# WebSocket support for real-time collab (Item #30). Pure-Python adapter on
# top of simple_websocket; works under the same gunicorn/Flask process as
# the rest of the API.
flask-sock
# Yjs CRDT in Python — server holds its own copy of each collab room's Y.Doc
# so updates persist across restarts and late joiners can sync to the
# canonical state. Rust-backed binding; pre-built wheels on PyPI for Win/Mac/Linux.
y-py
google-genai
manim
openai
langchain
langchain-openai
numpy
sympy
python-dotenv
matplotlib
pytest
pytest-mock
# YAML parser — used by test_docker_compose.py and by future ops tooling.
# manim transitively pulls this in but the dep is fragile to upstream changes;
# pin it here so a fresh install doesn't break the test suite.
pyyaml
