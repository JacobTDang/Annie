flask
flask-cors
# Production WSGI server — used in containerized deploys (Fly.io etc.). The
# Flask dev server is not safe for production traffic.
gunicorn
google-genai
manim
openai
langchain
langchain-openai
numpy
sympy
python-dotenv
matplotlib
pytest
pytest-mock
# YAML parser — used by test_docker_compose.py and by future ops tooling.
# manim transitively pulls this in but the dep is fragile to upstream changes;
# pin it here so a fresh install doesn't break the test suite.
pyyaml
